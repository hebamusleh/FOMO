import { IContactBody } from "@/models";

export const deleteAccountAPI = async (userId: string) => {
  try {
    // Implement the API call to delete a user account
  } catch (e) {
    // Handle error
  }
};

export const updateUserProfileAPI = async (userId: string, body: any) => {
  try {
    // Implement the API call to update user profile
  } catch (e) {
    // Handle error
  }
};

export const contactAPI = async (body: IContactBody) => {
  try {
    // Implement the API call to contact support
  } catch (e) {
    // Handle error
  }
};
